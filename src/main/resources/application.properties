spring.application.name=JobStuffAgent

# Required for local HTTP Basic authentication. Do not commit real credentials.
app.security.read-only-username=${APP_SECURITY_READ_ONLY_USERNAME:}
app.security.read-only-password=${APP_SECURITY_READ_ONLY_PASSWORD:}
app.security.manager-username=${APP_SECURITY_MANAGER_USERNAME:}
app.security.manager-password=${APP_SECURITY_MANAGER_PASSWORD:}
